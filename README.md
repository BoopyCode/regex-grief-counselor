# Regex Grief Counselor

Helps debug regular expressions with therapeutic commentary. Run: `python regex_grief_counselor.py "pattern" "test_string"` or just `python regex_grief_counselor.py` for interactive mode. Because regex shouldn't make you cry.